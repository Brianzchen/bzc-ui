// @flow
import * as React from 'react';
import ReactDOM from 'react-dom';

const ele = document.getElementById('root');

if (ele) {
  ReactDOM.render(
    <div>
      Starfall!
    </div>,
    ele,
  );
}
