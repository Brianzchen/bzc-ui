// @flow
import * as React from 'react';

import { Card } from '..';

export const Basic = (): React.Node => (
  <Card
    style={{
      height: '200px',
      width: '200px',
      margin: 'auto',
    }}
  >
    A Card looks like this
  </Card>
);
